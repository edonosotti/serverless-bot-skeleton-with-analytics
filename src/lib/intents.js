'use strict';

module.exports = {
  // Values *MUST* match the "action" parameters
  // set in Dialogflow for each available Intent
  UNKNOWN: 'input.unknown',
  WELCOME: 'input.welcome',
  MAGIC_TRICKS: 'input.magic_tricks',
}
